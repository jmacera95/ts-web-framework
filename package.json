{
  "name": "ts-web-framework",
  "source": "./index.html",
  "scripts": {
    "start:db": "npx json-server -w db.json",
    "start:parcel": "npx parcel index.html",
    "build": "parcel build"
  },
  "devDependencies": {
    "parcel": "latest"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "json-server": "^0.17.4"
  }
}
